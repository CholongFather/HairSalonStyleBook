<footer class="salon-footer">
    <div class="container text-center">
        <div class="footer-ornament">
            <span class="footer-line"></span>
            <i class="bi bi-scissors"></i>
            <span class="footer-line"></span>
        </div>
        <p class="footer-brand">늘-오던 헤어살롱</p>
        <p class="footer-tagline">Style Book</p>
        <p class="footer-copy">&copy; @DateTime.Now.Year All Rights Reserved</p>
    </div>
</footer>

<style>
    .salon-footer {
        background-color: var(--salon-black);
        color: var(--salon-light-gray);
        padding: 2.5rem 0 2rem;
        margin-top: 3rem;
    }

    .footer-ornament {
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 0.8rem;
        margin-bottom: 1.2rem;
    }

    .footer-ornament i {
        color: var(--salon-accent);
        font-size: 0.8rem;
        opacity: 0.5;
    }

    .footer-line {
        width: 40px;
        height: 1px;
        background: linear-gradient(to right, transparent, var(--salon-accent));
        opacity: 0.3;
    }

    .footer-line:last-child {
        background: linear-gradient(to left, transparent, var(--salon-accent));
    }

    .footer-brand {
        font-family: var(--font-serif);
        font-size: 1rem;
        font-weight: 700;
        letter-spacing: 0.15em;
        margin-bottom: 0.15rem;
    }

    .footer-tagline {
        font-family: var(--font-serif);
        font-size: 0.65rem;
        letter-spacing: 0.3em;
        text-transform: uppercase;
        opacity: 0.4;
        margin-bottom: 0.8rem;
    }

    .footer-copy {
        font-size: 0.7rem;
        opacity: 0.35;
        margin: 0;
    }
</style>
