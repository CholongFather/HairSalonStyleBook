<div class="ribbon-divider">
    <div class="ribbon-line"></div>
    <div class="ribbon-bow">
        <i class="bi bi-flower1"></i>
    </div>
    <div class="ribbon-line"></div>
</div>

<style>
    .ribbon-divider {
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 0.25rem 1rem;
        background-color: var(--salon-white);
    }

    .ribbon-line {
        flex: 1;
        height: 1px;
        background: linear-gradient(to right, transparent, var(--salon-green), transparent);
        max-width: 200px;
    }

    .ribbon-bow {
        margin: 0 1rem;
        color: var(--salon-green);
        font-size: 0.8rem;
    }
</style>
